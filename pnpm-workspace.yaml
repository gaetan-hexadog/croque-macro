# pnpm ≥ 10 : les scripts de build des dépendances sont bloqués par défaut.
# esbuild (utilisé par vite/vitest) a besoin du sien pour installer son binaire.
allowBuilds:
  esbuild: true
onlyBuiltDependencies:
  - esbuild
